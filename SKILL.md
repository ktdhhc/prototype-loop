---
name: prototype-loop
description: 逐层原型设计单个界面。用于用户尚未确定界面方案、希望先看可切换原型再逐层决策，或要求将界面设计从澄清推进到可接线交付时。
---

# Prototype Loop

用可见原型逐层确定界面设计。**轴**是可独立变化的设计维度；**层**是本轮交给用户决策并冻结的工作单元。每层只有一个可比较的决策点；未决定的部分留占位。过程账本记录变化，收口文档记录可交付的结论。

## 0. 检查依赖，报告后启动

开始前检查并加载 `impeccable`、`ui-ux-pro-max`、`prototype`、`grilling`，确认一次只问一个问题的提问工具可用；报告各项结果。遵循这些技能当前版本的原始用法，不在这里复制其方法。

- 四项技能与提问工具齐备：报告就绪，进入步骤 1。
- 缺少 `impeccable` 或 `ui-ux-pro-max`：报告缺项及相应的访谈或检索降级方式，等用户决定是否继续；继续时标明未执行的依赖环节。
- 缺少 `prototype`、`grilling` 或逐题提问工具：报告阻断原因，停在依赖检查。本流程的可见原型和逐题决策无法由文字猜测替代。

完成判据：依赖状态已报告；标准路径全部就绪，或降级路径已获用户选择。

## 1. 确定产品事实与视觉依据

用 `impeccable init` 核对产品事实，辨认已有实现、`PRODUCT.md`、`DESIGN.md` 与实际视觉依据。已有文档时，先让用户在**沿用、按现有依据试画对照、重新澄清**三条路径中选择；不要因文件存在就跳过核对。重开时先保留旧文档作为证据，获得确认后再修改。遵守项目对基线文档的写入约束。

组件库未定时，在界面访谈前读取 [组件库采样](references/sampler.md)：同样的 7–10 组元素按候选库分别渲染，只改变组件库，让用户看过后再决定。即使有 `DESIGN.md`，用户选择“试画对照”也可采样。已有视觉依据但组件库已定，则按所选路径沿用或验证，不额外造一套视觉世界。

完成判据：产品事实与视觉依据已确认或明确延后；组件库已选定或明确延后；涉及文档的修改得到项目要求的授权。

## 2. 访谈目标界面

用 `impeccable shape` 澄清使用者、页面任务、成功结果、实际素材、行为、状态和边界，在目标项目建立过程账本，将结论写成界面简述，请用户确认。界面访谈决定**这页做什么、做到哪**；逐层提问才决定**具体怎么做**。阶段 1 的组件采样不替代界面访谈。

完成判据：界面简述有明确范围和真实素材，用户确认可据此拆层。

## 3. 检索设计依据

用 `ui-ux-pro-max` 按界面的不同意图分别检索；把有用发现映射到后续决策或既有约束。未命中时注明采用的其他依据，不把经验伪装成检索结果。

完成判据：本次检索的命中、未命中与适用范围已记录在过程账本。

## 4. 拆层并过闸门

读取 [拆层方法](references/layers.md) 与 [参考轴池](references/axis-pool.md)，将轴池、界面简述、检索结论及用户输入映射为当前页面的候选轴。按独立性判定与影响半径排序。先按 [账本模板](references/ledger-template.md)写入分层表**初版**，再为每层写 2–3 个仅差本层决策点的反事实变体；不通过就重拆。层数超过 12 时，给出拆成两个页面或两大轮的建议，由用户决定。

参考轴池是静态示例，不由其他项目的结论回填；本项目新发现的轴只进入本项目账本。

完成判据：账本含初版分层表、逐层反事实结果、依赖顺序与跨层待办；用户认可分层后才开始原型。

## 5. 一层一层做原型

每层读取 [原型循环](references/prototype-loop.md) 与 [逐题对齐](references/questions.md)：锁住其他轴制作可切换原型 → 在真实渲染中验收 → 只问会改变做法的未定问题 → 记录结论并冻结。设计纪律的执行错误当场修正；属于未来层的想法入账本；推翻已定结论时回退受影响的层。涉及视觉依据的变化记为 `ΔDESIGN`。

**每层冻结后停止**，告知本层结论和下一层的决策点，等待用户放行；只有用户明确授权连续推进才可在授权范围内连做。

完成判据：当前层的变体确实只差一个决策点，用户看过可用渲染并确认本层结论，未定问题有结论或被明确延后，账本有定稿记录；所有层完成后才进入收口。

## 6. 收口与交付

读取 [收口清单](references/closure.md)。先汇总账本里的 `ΔDESIGN`，与现有 `DESIGN.md` 对照，把冲突与新增建议交用户确认后再修订。随后清理原型脚手架、核对状态和可访问性、验证主路径与受影响构建，产出给正式开发接线的页面设计说明。没有正式接口的演示能力保持诚实标记。

完成判据：视觉依据的差异有经确认的去向；原型残留已清理；适用验证通过；交付说明包含已定决策、功能、数据接缝与未决边界。

## 过程账本

在界面访谈时建立 `.scratch/prototype/<页面名>/LAYER-DECISIONS.md`，拆层时写入初版分层表与反事实测试，随后记录每层的问答、回退、跨层待办及 `ΔDESIGN`。它是**可修订的过程记录，不是正式规格**；收口时把确定的结论提炼进交付文档，不把历史争论复制成产品规则。参照 [账本模板](references/ledger-template.md)。
