# 过程账本模板

> 这是 prototype-loop 在当前目标项目内复制使用的账本骨架：界面访谈时建立，拆层时补齐初版与测试，贯穿逐层原型与收口。项目之间不回填设计结论。
> 位置约定：`.scratch/prototype/<页面名>/LAYER-DECISIONS.md`。性质：**非铁律**——只作为最终原型合并与后续接线的交互参考，随时可能变化；不是 Spec、不是 ADR，不进入正式验收基线。

```markdown
# <页面名>：分层设计决策记录

> 性质：当前项目的过程账本，不是正式规格。收口时将确认过的结论提炼进本项目交付说明；本文件按项目约定保留或清理。
> 规则：每层一个决策点；未决策处留空位占位；每层原型完成后经渲染验收与逐题对齐，冻结后停下等用户放行。

## 界面简述与范围

- 使用者、页面任务与成功结果：
- 真实素材、行为和主要状态：
- 范围边界及已有视觉依据：

## 分层表初版

| 层 | 唯一决策点 | 明确不决定 | 影响到的后续层 | 状态 |
| --- | --- | --- | --- | --- |
| L1 … | … | … | … | 未开始 / 进行中 / 已定稿 |

## 逐层反事实测试

| 层 | 2–3 个单差变体 | 测试结果与修订 |
| --- | --- | --- |
| L1 … | … | … |

分层闸门：用户是否认可初版及修订记录。

前置闸门：本次范围边界（如：只跑主路径 / 含编辑 / 只读）——影响后续层原型要不要含编辑态。
收口（不占层）：走查、诚实标记、文档增补、原型清理。

## 约束输入

- 检索结论（阶段 3）：每条标注来源（数据库命中 / 内置规则）。
- 设计系统约束（DESIGN.md 条款引用）。

## L<n> <层名> —— <状态>

原型：变体与切换方式；本层唯一差异。
渲染验收：用户看过的结果、修正和仍未决定的问题。

| 决定性问题 | 用户回答 | 决策影响 |
| --- | --- | --- |

定稿结论与理由：
推翻记录（如有）：原结论 → 原因 → 新结论。
冻结后的下一层预告与用户放行记录：

## 跨层待办输入

| 事项 | 归属层 | 来源 |
（用户新输入先归类再入表；处理完划掉并注明去向）

## ΔDESIGN 清单

| 决策 | 建议（补条目 / 修订条款 / 冲突提醒） | 收口去向 |
（收口 6.1 逐条经用户确认后回写 DESIGN.md）

## 过程反馈

用户对流程本身的反馈（如提问节奏、原型形态），供下次改进。
```

## 使用要点

1. **访谈时建立**：先记录界面简述；拆层时就将分层表初版与变体证明写进去，不到收口才补写。
2. **逐题记录决定性问答**：答案及其对做法的影响写入当前层；没有未定问题时记录零提问的依据。
3. **`ΔDESIGN` 及时标**：层冻结时凡涉及视觉权威的决策当场标记，收口 6.1 才扫得出来。
4. **跨层待办是唯一的中转站**：不属于当前层的用户输入一律先入表，到所属层再处理——处理路径只有两条：账本里，或所属层的执行里。
